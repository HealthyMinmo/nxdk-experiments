XBE_TITLE = nxdk\ sample\ -\ quads_YUV420
GEN_XISO = $(XBE_TITLE).iso
# Use a relative source path to avoid issues when the directory name contains spaces
SRCS = main.c
SHADER_OBJS = ps.inl vs.inl
NXDK_DIR ?= $(CURDIR)/../..

include $(NXDK_DIR)/Makefile
